# Tasks

Task folders are the durable execution record for chat-driven work.

- `active/`: in-progress or blocked tasks
- `completed/`: finished tasks
- `index.md`: high-level registry of all tasks

Every meaningful task should exist here, even if the result is "failed to reproduce".
